% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters_core.R
\name{tpt_filter}
\alias{tpt_filter}
\title{Filter a two-point result (HSMap.tpt) by RF/LOD, with optional pre-ordering}
\usage{
tpt_filter(
  x,
  order = NULL,
  thresh.LOD.ph = 5,
  thresh.LOD.rf = 5,
  thresh.rf = 0.15,
  probs = c(0.05, 1),
  diag.markers = NULL,
  diagnostic.plot = TRUE,
  breaks = 100
)
}
\arguments{
\item{x}{An object of class \code{"HSMap.tpt"} whose matrices live under
\code{x$fit}: \code{r}, \code{lod_r}, \code{lod_ph}, \code{logLik},
and \code{mom_phase_list}.}

\item{order}{Optional character vector of marker IDs to \strong{reindex} the
matrices before filtering (useful if you already have a map order).
Markers not present are ignored (you'll get a warning); the filter then
runs on the reindexed square submatrix.}

\item{thresh.LOD.ph}{Numeric. Kept for parity; we actually screen by
\code{lod_r}. The effective LOD cutoff is
\code{max(thresh.LOD.ph, thresh.LOD.rf)}. Default \code{5}.}

\item{thresh.LOD.rf}{Numeric LOD threshold for linkage (vs \eqn{r=0.5}).
Combined with \code{thresh.LOD.ph} by \code{max()}. Default \code{5}.}

\item{thresh.rf}{Numeric RF cutoff; only pairs with \code{r <= thresh.rf}
are considered informative. Default \code{0.15}.}

\item{probs}{Numeric length-2 vector of quantiles for the \strong{row non-NA counts}
after masking (see Details). Markers outside \code{[probs[1], probs[2]]}
are dropped. Default \code{c(0.05, 1)} keeps the top 95\% by information.}

\item{diag.markers}{Optional non-negative integer half-bandwidth. If supplied,
we keep only entries with \code{|i - j| <= diag.markers} \strong{after any reordering}.
This is useful when you only want to keep near-diagonal (putative linked) pairs.}

\item{diagnostic.plot}{Logical. If \code{TRUE}, plot histograms of the per-marker
non-NA counts before/after keeping markers. Default \code{TRUE}.}

\item{breaks}{Integer number of bins for the diagnostic histogram. Default \code{100}.}
}
\value{
The same object \code{x} (class \code{"HSMap.tpt"}) with:
\itemize{
\item \code{x$fit$r}, \code{x$fit$lod_r}, \code{x$fit$lod_ph}, \code{x$fit$logLik}
subset to the kept markers (square, same order).
\item \code{x$fit$mom_phase_list[[g]]} subset similarly for each population.
\item \code{x$fit$markers} and \code{x$markers} updated to the kept order.
\item \code{x$filter_stats}: list with thresholds, counts, and \code{kept_markers}.
}
}
\description{
This function filters the \strong{two-point} output from HSMap by:
\enumerate{
\item optionally reordering markers by a user-supplied sequence \code{order},
\item keeping only pairs with LOD >= threshold and RF <= threshold
(optionally within a diagonal band around the matrix diagonal), and
\item retaining only those markers that have a sufficient number of surviving,
non-\code{NA} pairs (controlled by a row-count quantile window).
}

It returns the same \code{"HSMap.tpt"} object, \strong{restricted to the retained
markers} and preserving the chosen order. All companion matrices
(\code{r}, \code{lod_r}, \code{lod_ph}, \code{logLik}) and each element of \code{mom_phase_list}
are subset consistently. The top-level fields \code{markers} and \code{fit$markers}
are updated. A \code{filter_stats} list is attached with thresholds and the kept IDs.
}
\details{
\strong{Masking logic.} We build a logical mask \eqn{M_{ij}} that is \code{TRUE} when:
\deqn{ \text{LOD}_{ij} \ge \text{LOD\_thr} \;\;\text{and}\;\; r_{ij} \le \text{RF\_thr} }
where \eqn{\text{LOD\_thr} = \max(\text{thresh.LOD.ph}, \text{thresh.LOD.rf})}
and \eqn{\text{RF\_thr} = \text{thresh.rf}}. If \code{diag.markers} is given,
we further intersect with the diagonal band \eqn{|i-j|\le \text{diag.markers}}.

For each marker \(i\), we count how many \code{TRUE} entries remain in row \(i\).
We then keep markers whose count lies within the quantile window
\code{[probs[1], probs[2]]} of this distribution. This drops outlier rows that are
\strong{too sparse} (uninformative) or, if you set the upper quantile < 1, \strong{too dense} (often
indicative of artefacts).
}
\examples{
\dontrun{
# Keep near-diagonal informative pairs and the best 90\% markers by row counts
tpt2 <- tpt_filter(
  x = tpt,
  order = known_order,          # optional pre-order
  thresh.LOD.rf = 6,
  thresh.rf     = 0.2,
  diag.markers  = 200,
  probs         = c(0.10, 1),
  diagnostic.plot = TRUE
)
ncol(tpt2$fit$r)   # number of kept markers
}
}

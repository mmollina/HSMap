% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_methods.R
\name{plot.hsmap_group}
\alias{plot.hsmap_group}
\title{Plot an hsmap_group as a colored dendrogram with cluster rectangles}
\usage{
\method{plot}{hsmap_group}(x, border = "black", lwd_base = 1.5, palette = hs_pal, ...)
}
\arguments{
\item{x}{An object of class \code{"hsmap_group"}.}

\item{border}{Rectangle border color. Default \code{"black"}.}

\item{lwd_base}{Base line width for the dendrogram. Default \code{1.5}.}

\item{palette}{A function \code{f(n)} returning \code{n} colors (default \code{\link[=hs_pal]{hs_pal()}}).}

\item{...}{Passed to \code{plot.dendrogram()}.}
}
\value{
Invisibly returns a list of length \code{k}, each the marker names inside
each rectangle from left to right.
}
\description{
Colors branches by group (using \code{palette(k)}), draws \code{rect.hclust()} boxes,
and overlays group sizes mappoly2-style. Useful to visually assess the cut.
}

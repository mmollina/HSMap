% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fullsib_core.R
\name{hmm_map_mixed}
\alias{hmm_map_mixed}
\title{Fit a mixed open-pollinated + known-sire recombination map}
\usage{
hmm_map_mixed(
  x,
  phased_m,
  phased_p = NULL,
  order = NULL,
  epsilon = 0.05,
  lambda = 20,
  q0 = 0.5,
  tol = 1e-06,
  maxit = 1000L,
  r_start = 0.1,
  gap_r = 0.499,
  untyped_sire = c("error", "open_pollinated"),
  on_missing = c("error"),
  return_posterior = FALSE
)
}
\arguments{
\item{x}{An \code{HSMap.data} object with cross-aware fields.}

\item{phased_m}{Named list: mother id -> maternal phase vector (length z-1).}

\item{phased_p}{Named list: sire id -> paternal phase vector (required if any
full-sib cross is fitted).}

\item{order}{Optional marker order; default the common genotype column order.}

\item{epsilon, tol, maxit, r_start}{As in \code{hmm_map_fullsib}.}

\item{lambda, q0}{OP paternal-\code{q} pseudocount total and shrinkage target
(MAP update), matching \code{hmm_map}.}

\item{gap_r}{Recombination fraction at/above which an interval is a no-linkage
boundary (gap): map distance \code{NA}. Default \code{0.499}.}

\item{untyped_sire}{How to treat \code{known_sire_untyped} crosses: \code{"error"}
(default) stop; \code{"open_pollinated"} model them with the OP model (reported).}

\item{on_missing}{\code{"error"} (default) on a missing required parent genotype.}

\item{return_posterior}{Logical; include full-sib posterior inheritance probabilities.}
}
\value{
An object of class \code{HSMap.mixed}.
}
\description{
Estimate a shared maternal recombination map from a dataset containing both
open-pollinated (OP) and known-sire full-sib crosses, plus a separate shared
paternal map from the full-sib crosses. OP crosses use the two-state maternal model
with a dam-specific paternal gametic frequency \code{q}; full-sib crosses use the
four-state model. With only OP crosses present, dispatches to the existing engine so
OP-only results are unchanged. See \code{dev/known_sire_design.md}.
}

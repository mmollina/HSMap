% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_multi_chrom.R
\name{sim_multi_chrom}
\alias{sim_multi_chrom}
\title{Simulate multi-population data across multiple chromosomes}
\usage{
sim_multi_chrom(
  map,
  n_pops = 2,
  n_ind_per_pop = rep(50, n_pops),
  marker_intersection = 1,
  phase_mode = c("all_coupling", "random", "vector"),
  repulsion_rate = 0.2,
  phase_vector = NULL,
  maternal_geno_mode = c("HWE", "all_het", "vector"),
  maternal_M_given = NULL,
  maternal_pA = 0.5,
  paternal_pA_base = 0.5,
  paternal_pA_sd = 0,
  known_sire_prob_per_pop = 0,
  error_rate = 0,
  pedigree = NULL,
  seed = NULL,
  keep_paths = FALSE,
  miss_rate = 0
)
}
\arguments{
\item{map}{A data frame returned by \code{make_map()} (or equivalent) containing
at least columns \code{marker_id}, \code{chrom}, \code{position}, and the
attribute \code{"r_vec"} (a length-\code{T-1} numeric vector of recombination
fractions across the genome).}

\item{n_pops}{Integer. Number of populations (nuclear families) to simulate.}

\item{n_ind_per_pop}{Integer vector of length \code{n_pops}. Number of offspring per population.}

\item{marker_intersection}{Numeric in \verb{[0,1]}. Fraction of markers shared by all populations.
Non-shared markers are scattered across the non-intersection while preserving order.}

\item{phase_mode}{Character, one of \code{"all_coupling"}, \code{"random"}, \code{"vector"}.}

\item{repulsion_rate}{Numeric in \verb{[0,1]}. Used only when \code{phase_mode = "random"}, the
fraction of intervals simulated in repulsion.}

\item{phase_vector}{Integer vector of length \code{T_markers - 1} with entries \code{0/1},
used only when \code{phase_mode = "vector"}. Ignored otherwise.}

\item{maternal_geno_mode}{Character, one of \code{"HWE"}, \code{"all_het"}, \code{"vector"}.}

\item{maternal_M_given}{Optional integer vector (0/1/2/NA) of length \code{T_markers}
or named by union markers; used only when \code{maternal_geno_mode = "vector"}.}

\item{maternal_pA}{Numeric scalar or length-\code{T_markers} vector in \verb{(0,1)} for HWE
maternal genotypes when \code{maternal_geno_mode = "HWE"}. Values are clipped to \verb{(1e-6, 1-1e-6)}.}

\item{paternal_pA_base}{Numeric scalar or length-\code{T_markers} vector in \verb{(0,1)} for HWE
paternal mixture per marker. Values are clipped to \verb{(1e-6, 1-1e-6)} before population jitter.}

\item{paternal_pA_sd}{Non-negative numeric. Per-population Gaussian perturbation SD
added to \code{paternal_pA_base} \emph{per marker}. The perturbed values are then
truncated into \verb{(1e-4, 1-1e-4)}.}

\item{known_sire_prob_per_pop}{Numeric in \verb{[0,1]}. When no pedigree is passed, each
population independently has a known sire with this probability. \strong{This is a
simulation-only, fixed paternal-genotype mechanism:} the sire's genotype is drawn
independently per marker (no linked paternal meiosis, no paternal haplotype), and it
does \strong{not} imply or enable known-sire / full-sib mapping --- the estimators
here model the open-pollinated / unknown-sire case only.}

\item{error_rate}{Numeric in \verb{[0,1]}. Per offspring/marker error rate for replacing
a generated genotype with a different value.}

\item{pedigree}{Optional \code{data.frame} with columns \verb{id, mother, father, generation, family_id}.
If provided, families are derived from the pedigree; all families are assigned the full
union marker set in order.}

\item{seed}{Optional integer for reproducibility.}

\item{keep_paths}{Logical; if \code{TRUE}, the hidden paths \code{H} per population are returned.}

\item{miss_rate}{Numeric scalar in \verb{[0,1]} or length-\code{n_pops} vector. Additional
random missingness (MCAR) applied per population to offspring genotypes.
Default 0 (no extra missingness).}
}
\value{
An object of class \code{"sim_multi_pop"} identical in structure to
\code{sim_multi_pop()}, with the truth bundle augmented by \code{chrom} and
\code{position} per marker:
\itemize{
\item \code{truth$map}: the input \code{map} with the same row order as \code{markers_union}.
}
}
\description{
Genome-aware wrapper around \code{sim_multi_pop()} that accepts a multi-chromosome
map (from \code{make_map()}) and simulates across all markers in order. Between
chromosomes, the recombination fraction is fixed to 0.5 so blocks are independent.
}
\examples{
\dontrun{
map <- make_map(2, c(10, 12), r_const_per_chrom = c(0.02, 0.03))
sim <- sim_multi_chrom(
  map, n_pops = 2, n_ind_per_pop = c(60, 50),
  maternal_geno_mode = "HWE", maternal_pA = 0.5,
  paternal_pA_base = 0.4, error_rate = 0.01, seed = 7
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_core.R
\name{group_markers}
\alias{group_markers}
\title{Group markers into linkage groups from two-point RF results (HSMap)}
\usage{
group_markers(
  w,
  k = NULL,
  inter = TRUE,
  compare = FALSE,
  weight_by_lod = FALSE,
  chrom = NULL,
  method = "average",
  na_r = 0.5,
  palette = hs_pal
)
}
\arguments{
\item{w}{Either:
\itemize{
\item a \strong{list} with components \code{r} and \code{lod_r}, both square numeric matrices
with identical row/column names; or
\item an object of class \code{"HSMap.tpt"} whose \verb{$fit} contains such a list.
}}

\item{k}{Integer. Desired number of linkage groups. If \code{NULL} and \code{inter = TRUE},
you can \strong{choose interactively}; otherwise an error is thrown.}

\item{inter}{Logical. If \code{TRUE} and running interactively, show a dendrogram
preview (using \code{\link[=plot.hsmap_group]{plot.hsmap_group()}}) and prompt for \code{k}. Default \code{TRUE}.}

\item{compare}{Logical. If \code{TRUE} and \code{chrom} is provided, attach a \strong{comparison
matrix} (LG × chromosome) to the result (\verb{$comp}). Default \code{FALSE}.}

\item{weight_by_lod}{Logical. If \code{TRUE}, compute per-marker weights
\eqn{w_i = \mathrm{round}(\mathrm{mean}_j[\mathrm{LOD}_{ij}^2])} and pass them
as \code{members} to \code{hclust}. This tends to boost well-supported markers and can
improve cluster stability. If you use weights and \code{method = "average"},
we switch to \code{method = "ward.D2"} (safer with \code{members}). Default \code{FALSE}.}

\item{chrom}{Optional \strong{named} character vector giving chromosome labels per
marker (names must match the matrix dimnames). Only used if \code{compare = TRUE}.}

\item{method}{Character linkage method for \code{stats::hclust}; one of
\code{"average"}, \code{"complete"}, \code{"single"}, \code{"ward.D2"}, etc. Default \code{"average"}.}

\item{na_r}{Numeric in [0, 0.5]. Value used to fill \code{NA} in \code{r} before
clustering. Default \code{0.5} (uninformative / unlinked).}

\item{palette}{A function \code{f(n)} returning \code{n} colors to color dendrogram branches
(used by \code{\link[=plot.hsmap_group]{plot.hsmap_group()}}). Default: \code{\link[=hs_pal]{hs_pal()}}.}
}
\value{
An object of class \code{"hsmap_group"} with fields:
\describe{
\item{hc}{An \code{hclust} tree built from the cleaned RF matrix.}
\item{k}{Integer; number of groups used in the final cut.}
\item{groups}{Integer vector, names = marker IDs, giving LG membership.}
\item{markers}{Character; marker IDs used.}
\item{method}{Clustering linkage method.}
\item{comp}{If \code{compare = TRUE} and \code{chrom} was given, a LG × chromosome table.}
}
}
\description{
Clusters markers into \strong{linkage groups (LGs)} using pairwise recombination
fractions from HSMap’s two-point analysis. You can pass either:
\itemize{
\item the raw list returned by \code{"cpp_pairwise_rf()"} (must contain square matrices
\code{r} and \code{lod_r} with matching dimnames), or
\item an HSMap two-point wrapper result (class \code{"HSMap.tpt"}, whose \verb{$fit} field
is that same list).
}

The function first symmetrizes and cleans the RF matrix, converts it to a
distance (\code{as.dist} on the symmetrized \code{r}), then performs hierarchical
clustering with \code{stats::hclust}. Optionally, it uses \strong{LOD-derived weights}
to stabilize clustering if you have uneven information content across markers.

If \code{inter = TRUE} in an interactive session and \code{k} is not specified,
you’ll get an iterative \strong{dendrogram preview} to choose \code{k} (number of groups).
}
\details{
\subsection{What the algorithm does}{

Two-point analysis yields \eqn{r_{ij} \in [0, 0.5]} and typically a LOD score
for linkage vs \eqn{r = 0.5}. We:
\enumerate{
\item \strong{Symmetrize} \eqn{r} by \eqn{\tilde r = (r + r^\top)/2}, set the diagonal to 0,
clamp to [0, 0.5], and replace \code{NA} by \code{na_r} (default 0.5).
\item Treat \eqn{\tilde r} as a dissimilarity and feed \verb{as.dist(\\tilde r)} to
\code{stats::hclust(method = ...)}.
\item Optionally apply \strong{LOD-based weights}: for marker \eqn{i}, compute
\eqn{w_i = \mathrm{round}(\mathrm{mean}_j[\mathrm{LOD}_{ij}^2])} and pass
\code{members = w} to \code{hclust}. With \code{members}, \code{"ward.D2"} is typically more
appropriate than \code{"average"}; we auto-switch in that case.
\item \strong{Cut} the tree at \code{k} to obtain LG membership.
}
}

\subsection{Choosing \code{k}}{
\itemize{
\item If \code{k} is known (e.g., number of chromosomes), provide it directly.
\item If not, set \code{inter = TRUE} and pick \emph{visually}: we’ll color the dendrogram
by group and draw rectangles (mappoly2-style).
}
}

\subsection{Output object}{

The return is an \code{"hsmap_group"} object with the hclust tree, final \code{k},
per-marker group calls, (optionally) the comparison matrix, and metadata. It
has friendly \code{print()} and \code{plot()} methods (see \strong{Value}).
}
}
\examples{
\dontrun{
# Suppose `w` is the list returned by cpp_pairwise_rf()
g <- group_markers(w, k = 10)
print(g)
plot(g)                 # colored dendrogram w/ rectangles

# Interactive choice of k
g2 <- group_markers(w, inter = TRUE)  # choose k in the prompt

# With chromosome labels, get a comparison matrix
chr <- setNames(sim$truth$map$chrom, sim$truth$map$marker_id)
g3  <- group_markers(w, k = 10, compare = TRUE, chrom = chr)
g3$comp
}
}
\seealso{
\code{"cpp_pairwise_rf()"} for producing two-point RF/LOD matrices;
\code{\link[=plot.hsmap_group]{plot.hsmap_group()}} and \code{\link[=print.hsmap_group]{print.hsmap_group()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_plot.R
\name{plot_map_list}
\alias{plot_map_list}
\title{Plot one or more HSMap maps as linkage-group tracks}
\usage{
plot_map_list(
  x,
  horiz = FALSE,
  map.function = c("haldane", "kosambi", "morgan"),
  col = hs_pal(length(x))
)
}
\arguments{
\item{x}{An object of class \code{HSMap.map} or a list of such objects.
If a list is named, those names are used as linkage-group labels.}

\item{horiz}{Logical. Plot tracks horizontally if \code{TRUE}, vertically if \code{FALSE}.}

\item{map.function}{Character. One of \code{"haldane"}, \code{"kosambi"}, or \code{"morgan"}.
Used to transform recombination fractions to map distance in centiMorgans.}

\item{col}{A vector of colors for the tracks. Recycled as needed.
Defaults to \code{hs_pal(length(x))}.}
}
\value{
Invisibly returns a data frame with columns:
\code{mrk} (marker id), \code{LG} (linkage-group label), and \code{pos} (position in cM).
}
\description{
Plot a single \code{HSMap.map} or a list of \code{HSMap.map} objects as simple tracks.
Each track is a rounded rectangle representing the linkage group with small
ticks at marker positions. The function returns a tidy data frame with the
plotted positions (invisibly), which can be useful for labeling or custom
annotations.
}
\examples{
\dontrun{
# One map:
plot_map_list(my_map)

# Multiple maps side by side:
plot_map_list(list(LG1 = map1, LG2 = map2), horiz = TRUE, map.function = "kosambi")
}

}

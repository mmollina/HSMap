% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_plot.R
\name{get_map}
\alias{get_map}
\title{Compute map positions for an HSMap.map}
\usage{
get_map(map, map.function = c("haldane", "kosambi", "morgan"), gap_r = 0.499)
}
\arguments{
\item{map}{An object of class \code{HSMap.map} containing \code{map$fit$r} (vector of
recombination fractions between consecutive markers) and \code{map$order}
(character vector with marker names in map order).}

\item{map.function}{Character. One of \code{"haldane"}, \code{"kosambi"}, or \code{"morgan"}.}

\item{gap_r}{Recombination fraction at/above which an interval is treated as a
no-linkage gap (distance \code{NA}, excluded from the map length). Default \code{0.499}.}
}
\value{
A named numeric vector of cumulative positions in cM
with names equal to \code{map$order}, carrying attributes \code{n_gaps} and \code{block}
(the segment index of each marker, which increments after every gap).
}
\description{
Convert recombination fractions to cumulative positions in cM according to
the chosen mapping function. This is the gap-aware way to ask "where is each
marker on this map?": intervals with no linkage or unresolved phase do not
become huge centimorgan distances, positions restart after each such gap, and
the attributes record where the gaps are. Prefer it over
\code{cumsum(inv_haldane(c(0, map$fit$r)))}, which silently sums across gaps.
}
\examples{
\dontrun{
pos <- get_map(map10, "haldane")
head(pos)
attr(pos, "n_gaps")
}
}
\seealso{
\code{\link[=plot_map_list]{plot_map_list()}} and \code{\link[=plot_map_lmv]{plot_map_lmv()}} for figures,
\code{\link[=get_block_map]{get_block_map()}} for a gap-aware interval table and map length.
}
